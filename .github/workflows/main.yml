name: Build Release APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Clonar el repositorio
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 🔥 Clona todo el historial, necesario para gradle wrapper

    # 2️⃣ Configurar JDK 17
    - name: Setup JDK
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '17'
        cache: 'gradle'

    # 3️⃣ Generar gradlew si no existe
    - name: Generate Gradle Wrapper
      run: |
        if [ ! -f "./gradlew" ]; then
          echo "📦 Generando gradlew..."
          ./gradle wrapper --gradle-version=8.4 --distribution-type=bin
          # Si no hay gradle instalado, usamos una alternativa
          if [ ! -f "./gradlew" ]; then
            gradle wrapper --gradle-version=8.4 --distribution-type=bin
          fi
        fi
        
        # Asegurar que gradlew tenga permisos de ejecución
        chmod +x ./gradlew
        echo "✅ gradlew listo"
        ls -la gradlew

    # 4️⃣ Restaurar keystore desde secreto
    - name: Restore Keystore
      run: |
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > ollama-release.keystore
        if [ -f "ollama-release.keystore" ]; then
          echo "✅ Keystore creado exitosamente"
          ls -la ollama-release.keystore
        else
          echo "❌ ERROR: No se pudo crear el keystore"
          exit 1
        fi

    # 5️⃣ Configurar variables de entorno
    - name: Set environment variables
      run: |
        echo "KEYSTORE_FILE=$PWD/ollama-release.keystore" >> $GITHUB_ENV
        echo "KEYSTORE_PASSWORD=${{ secrets.KEYSTORE_PASSWORD }}" >> $GITHUB_ENV
        echo "KEY_ALIAS=${{ secrets.KEY_ALIAS }}" >> $GITHUB_ENV
        echo "KEY_PASSWORD=${{ secrets.KEY_PASSWORD }}" >> $GITHUB_ENV

    # 6️⃣ Preparar gradlew (ahora seguro que existe)
    - name: Prepare gradlew
      run: |
        chmod +x ./gradlew
        # Convertir a line endings LF (Linux)
        sudo apt-get update && sudo apt-get install -y dos2unix
        dos2unix ./gradlew
        echo "✅ gradlew preparado"
        ./gradlew --version

    # 7️⃣ Construir APK de release
    - name: Build Release APK
      run: |
        ./gradlew assembleRelease \
          -Pandroid.injected.signing.store.file="$KEYSTORE_FILE" \
          -Pandroid.injected.signing.store.password="$KEYSTORE_PASSWORD" \
          -Pandroid.injected.signing.key.alias="$KEY_ALIAS" \
          -Pandroid.injected.signing.key.password="$KEY_PASSWORD"

    # 8️⃣ Verificar APK generado
    - name: Verify APK was built
      run: |
        # Buscar el APK en diferentes ubicaciones posibles
        APK_PATH=$(find . -name "*release*.apk" -type f | head -1)
        if [ -f "$APK_PATH" ]; then
          echo "✅ APK creado exitosamente: $APK_PATH"
          ls -la $(dirname "$APK_PATH")
        else
          echo "❌ No se encontró el APK. Buscando en todo el proyecto:"
          find . -name "*.apk" -type f
          exit 1
        fi

    # 9️⃣ Subir APK como artefacto
    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: Ollama-Server
        path: app/build/outputs/apk/release/*.apk
